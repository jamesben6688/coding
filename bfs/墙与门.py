"""

 什么叫“门不在交点上”？
原题默认门是放在格子的中心，比如这样：

[房间] [房间] [门] [房间]
但 follow-up 中，“门”可能在两个格子之间，比如：

[房间] — 门 — [房间]
这说明：

门在两个格子的“连接线上”（边缘/交界处），

这扇门能同时通往两个格子。

✅ 怎么处理这种情况？
方法一：半格建图法（推荐）
你可以把整个房间网格看作图，每个格子是一个节点。
如果门在两个格子之间（比如 (i, j) 和 (i, j+1) 之间），
那你就在图中连一条 权重为 0 的边。

然后从所有“门连接的格子”出发做 BFS/0-1 BFS。

✅ 步骤：
找出门所在位置（比如在 (i, j) 和 (i, j+1) 之间）

把这两个房间都当作“离门距离为 0” 的起点入队。

然后正常用 BFS 继续扩散。
"""